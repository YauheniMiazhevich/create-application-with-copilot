# Copy this file to .env and fill in actual values before running docker compose.
# NEVER commit .env with real secrets.
#
# Usage:
#   cp .env.docker.example .env
#   # Edit .env with your values
#   docker compose up -d

# ---------------------------------------------------------------------------
# PostgreSQL
# ---------------------------------------------------------------------------
POSTGRES_DB=backendapi_dev_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ---------------------------------------------------------------------------
# JWT (backend auth)
# SecretKey must be at least 32 characters long
# ---------------------------------------------------------------------------
JWT_SECRET_KEY=change_me_super_secret_key_at_least_32_chars_long
JWT_ISSUER=BackendApi
JWT_AUDIENCE=BackendApiClients
JWT_EXPIRATION_HOURS=4

# ---------------------------------------------------------------------------
# CORS – the origin(s) the backend will accept requests from
# In Docker Compose this should match the host:port users browse to
# ---------------------------------------------------------------------------
FRONTEND_ORIGIN=http://localhost:3000

# ---------------------------------------------------------------------------
# Frontend build – API base URL baked into the React bundle at docker build time
# Should match the publicly reachable backend address from the browser
# ---------------------------------------------------------------------------
REACT_APP_API_URL=http://localhost:5272/api

# ---------------------------------------------------------------------------
# Port mappings on the host (optional – defaults shown)
# ---------------------------------------------------------------------------
BACKEND_PORT=5272
FRONTEND_PORT=3000
